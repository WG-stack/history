# 显示状态机整理文档

## 概述

本项目实现了一个完整的LCD显示状态机系统，支持多种显示模式、单位切换、公差判断、模拟指针显示等功能。系统采用分层架构，包括显示缓冲区管理、图标状态控制、模式状态管理等。

## 1. 显示硬件架构

### 1.1 LCD硬件连接
- **COM线**: 4条 (COM0-COM3)
- **SEG线**: 26条 (SEG0-SEG25)
- **显示位置**: 6个数字位 (LCD_POSITION_1-LCD_POSITION_6)
- **图标系统**: 多种功能图标 (单位、模式、公差、指针等)

### 1.2 显示缓冲区
```c
static uint32_t lcd_display_buffer_new[4];  // COM0-COM3的显示缓冲区
static uint8_t decimal_point_positions[6];  // 小数点状态数组
```

## 2. 显示状态机结构

### 2.1 LCD显示状态结构体
```c
typedef struct {
    uint8_t is_metric;               // 是否公制单位 (1=MM, 0=IN)
    uint8_t mode;                    // 显示模式
    uint8_t resolution;              // 分辨率
    uint8_t decimal_position;        // 小数点位置
    uint8_t icons_status[LCD_ICON_MAX_NUM]; // 图标状态数组
} LCD_DisplayState_TypeDef;
```

### 2.2 全局显示状态机实例
```c
LCD_DisplayState_TypeDef lcd_display_state;  // LCD显示状态机实例
```

## 3. 图标系统

### 3.1 图标枚举定义
```c
typedef enum {
    LCD_ICON_MM = 0,        // 毫米单位图标
    LCD_ICON_IN,            // 英寸单位图标
    LCD_ICON_REL,           // 相对模式图标
    LCD_ICON_MAX,           // 最大值模式图标
    LCD_ICON_MIN,           // 最小值模式图标
    LCD_ICON_TIR,           // 跳动模式图标
    LCD_ICON_TOL,           // 公差图标
    LCD_ICON_OK,            // 公差OK图标
    LCD_ICON_NG,            // 公差NG图标
    LCD_ICON_PS_PLUS_PLUS,  // 指针正超限图标
    LCD_ICON_PS_PLUS_1,     // 指针正1图标
    // ... (更多指针图标)
    LCD_ICON_PS_MINUS_15,   // 指针负15图标
    LCD_ICON_PS_MINUS_MINUS // 指针负超限图标
} LCD_Icon_TypeDef;
```

### 3.2 图标映射关系
| 功能 | 图标类型 | 位置映射 |
|------|----------|----------|
| 单位显示 | MM/IN | COM0/SEG16, COM3/SEG15 |
| 模式显示 | REL/MAX/MIN/TIR/TOL | COM3/SEG13, COM0/SEG14, COM2/SEG14, COM1/SEG14, COM2/SEG13 |
| 公差显示 | OK/NG | COM0/SEG13, COM1/SEG13 |
| 指针显示 | PS系列 | COM1-COM4/SEG17-25 (19个指针图标) |

## 4. 显示状态机状态管理

### 4.1 初始化状态
```c
void LCD_DisplayState_Init(void)
{
    // 从测量状态机同步初始单位设置
    extern ZTJ_TypeDef measurement_state;
    lcd_display_state.is_metric = (measurement_state.unit == 0) ? 1 : 0;
    lcd_display_state.mode = 0;
    lcd_display_state.resolution = 0;
    lcd_display_state.decimal_position = 0;
    
    // 初始化所有图标状态为关闭
    for(int i = 0; i < LCD_ICON_MAX_NUM; i++) {
        lcd_display_state.icons_status[i] = 0;
    }
}
```

### 4.2 状态设置函数
```c
// 设置单位状态
void LCD_DisplayState_SetMetric(uint8_t is_metric);
void LCD_DisplayState_SetUnit(uint8_t unit);

// 设置显示模式
void LCD_DisplayState_SetMode(uint8_t mode);

// 设置分辨率
void LCD_DisplayState_SetResolution(uint8_t resolution);

// 设置小数点位置
void LCD_DisplayState_SetDecimalPosition(uint8_t position);

// 设置图标状态
void LCD_DisplayState_SetIcon(LCD_Icon_TypeDef icon, uint8_t enable);
uint8_t LCD_DisplayState_GetIconStatus(LCD_Icon_TypeDef icon);
```

## 5. 显示内容管理

### 5.1 数字显示
```c
// 7段数码管段码定义
static const uint8_t digit_patterns_new[10] = {
    0x3F, // 0: abcdef
    0x06, // 1: bc
    0x5B, // 2: abged
    0x4F, // 3: abgcd
    0x66, // 4: fgbc
    0x6D, // 5: afgcd
    0x7D, // 6: afebcd
    0x07, // 7: abc
    0x7F, // 8: abcdefg
    0x6F  // 9: abcdfg
};
```

### 5.2 小数点位置计算
```c
static uint8_t GetDecimalIconPosition(uint8_t is_metric, uint8_t res)
{
    if(is_metric) {
        switch(res) {
            case RESOLUTION_0_0005MM: return 2; // 0.5μm: 4位小数
            case RESOLUTION_0_001MM:  return 3; // 1μm: 3位小数
            case RESOLUTION_0_01MM:   return 4; // 0.01mm: 2位小数
            default: return 3;
        }
    } else {
        // 英制单位的小数点位置向左移动一位
        switch(res) {
            case RESOLUTION_0_0005MM: return 1; // 0.5μm: 5位小数
            case RESOLUTION_0_001MM:  return 2; // 1μm: 4位小数
            case RESOLUTION_0_01MM:   return 3; // 0.01mm: 3位小数
            default: return 2;
        }
    }
}
```

## 6. 显示模式状态机

### 6.1 模式显示逻辑
```c
static void lcd_show_mode_icon(MeasurementMode_TypeDef mode)
{
    // 先清除所有模式图标
    LCDDisplay_ClearIcon_New(LCD_ICON_REL);
    LCDDisplay_ClearIcon_New(LCD_ICON_MAX);
    LCDDisplay_ClearIcon_New(LCD_ICON_MIN);
    LCDDisplay_ClearIcon_New(LCD_ICON_TIR);
    LCDDisplay_ClearIcon_New(LCD_ICON_TOL);
    
    // 显示当前模式图标
    switch(mode) {
        case MEASUREMENT_MODE_REL:
            LCDDisplay_ShowIcon_New(LCD_ICON_REL, 1);
            break;
        case MEASUREMENT_MODE_MAX:
            LCDDisplay_ShowIcon_New(LCD_ICON_MAX, 1);
            break;
        case MEASUREMENT_MODE_MIN:
            LCDDisplay_ShowIcon_New(LCD_ICON_MIN, 1);
            break;
        case MEASUREMENT_MODE_TIR:
            LCDDisplay_ShowIcon_New(LCD_ICON_TIR, 1);
            break;
        case MEASUREMENT_MODE_TOL_SETTING:
            LCDDisplay_ShowIcon_New(LCD_ICON_TOL, 1);
            break;
        case MEASUREMENT_MODE_NORMAL:
            // NORMAL模式：不显示特殊模式图标
            break;
        default:
            break;
    }
}
```

### 6.2 公差显示逻辑
```c
static void lcd_show_tolerance_icon(int32_t display_value)
{
    if(measurement_state.is_tolerance_enabled) {
        ToleranceResult_TypeDef tol_result = ZTJ_GetToleranceResult(display_value);
        if(tol_result == TOLERANCE_RESULT_OK) {
            LCDDisplay_ShowIcon_New(LCD_ICON_OK, 1);
            LCDDisplay_ClearIcon_New(LCD_ICON_NG);
        } else {
            LCDDisplay_ClearIcon_New(LCD_ICON_OK);
            LCDDisplay_ShowIcon_New(LCD_ICON_NG, 1);
        }
    } else {
        LCDDisplay_ClearIcon_New(LCD_ICON_OK);
        LCDDisplay_ClearIcon_New(LCD_ICON_NG);
    }
}
```

## 7. 模拟指针显示状态机

### 7.1 指针值管理
```c
extern int8_t analog_pointer_value;  // 模拟指针值 (-15 到 +15)
```

### 7.2 指针显示逻辑
```c
// 在相对测量模式下显示指针
if(current_mode == MEASUREMENT_MODE_REL || current_mode == MEASUREMENT_MODE_NORMAL ||
   current_mode == MEASUREMENT_MODE_MAX || current_mode == MEASUREMENT_MODE_MIN ||
   current_mode == MEASUREMENT_MODE_TIR || current_mode == MEASUREMENT_MODE_TOL_SETTING) {
    
    // PS0图标始终显示
    LCDDisplay_ShowIcon_New(LCD_ICON_PS_ZERO, 1);
    
    // TIR模式特殊处理
    if(current_mode == MEASUREMENT_MODE_TIR) {
        // 根据最大正/负跳动显示相应的PS+/-图标
        int32_t max_positive_dev = ZTJ_GetTirMaxPositiveDeviation();
        int32_t max_negative_dev = ZTJ_GetTirMaxNegativeDeviation();
        
        // 计算当前偏差并显示相应指针
        int32_t current_deviation = processed_data.value - ZTJ_GetTirBaseValue();
        
        // 根据偏差比例点亮PS+或PS-图标
        if(current_deviation > 0) {
            // 点亮PS+图标
            float ratio = (float)current_deviation / max_positive_dev;
            int ps_level = (int)(ratio * 15);
            for(int i = 1; i <= ps_level; i++) {
                LCDDisplay_ShowIcon_New((LCD_Icon_TypeDef)(LCD_ICON_PS_PLUS_1 + i - 1), 1);
            }
            analog_pointer_value = ps_level;
        } else if(current_deviation < 0) {
            // 点亮PS-图标
            float ratio = (float)(-current_deviation) / (-max_negative_dev);
            int ps_level = (int)(ratio * 15);
            for(int i = 1; i <= ps_level; i++) {
                LCDDisplay_ShowIcon_New((LCD_Icon_TypeDef)(LCD_ICON_PS_MINUS_1 + i - 1), 1);
            }
            analog_pointer_value = -ps_level;
        } else {
            analog_pointer_value = 0;
        }
        
        // 超限指示
        if(max_positive_dev > 15) {
            LCDDisplay_ShowIcon_New(LCD_ICON_PS_PLUS_PLUS, 1);
        }
        if(max_negative_dev < -15) {
            LCDDisplay_ShowIcon_New(LCD_ICON_PS_MINUS_MINUS, 1);
        }
    } else {
        // 非TIR模式下按常规方式显示指针
        if(analog_pointer_value > 15) {
            LCDDisplay_ShowIcon_New(LCD_ICON_PS_PLUS_PLUS, 1);
        } else if(analog_pointer_value < -15) {
            LCDDisplay_ShowIcon_New(LCD_ICON_PS_MINUS_MINUS, 1);
        }
        
        // 根据指针值点亮相应图标
        if(analog_pointer_value > 0) {
            for(int i = 1; i <= analog_pointer_value; i++) {
                LCDDisplay_ShowIcon_New((LCD_Icon_TypeDef)(LCD_ICON_PS_PLUS_1 + i - 1), 1);
            }
        } else if(analog_pointer_value < 0) {
            for(int i = 1; i <= (-analog_pointer_value); i++) {
                LCDDisplay_ShowIcon_New((LCD_Icon_TypeDef)(LCD_ICON_PS_MINUS_1 + i - 1), 1);
            }
        }
    }
}
```

## 8. 统一显示接口

### 8.1 主要显示函数
```c
void LCDDisplay_ShowProcessedData(SensorData_TypeDef processed_data)
{
    // 1. 清屏初始化
    LCDDisplay_Clear_New();
    
    // 2. 获取显示参数
    uint8_t res = measurement_state.current_resolution;
    LCD_DisplayState_TypeDef* lcd_display_state_ptr = LCD_DisplayState_GetCurrent();
    uint8_t is_metric = lcd_display_state_ptr->is_metric;
    uint8_t is_negative = processed_data.sign;
    int32_t display_value = processed_data.value;
    
    // 3. 显示负号
    lcd_show_negative_sign(is_negative);
    
    // 4. 显示数字和小数点
    // 根据分辨率处理6位/非6位显示模式
    // 实现前导零消除功能
    
    // 5. 显示单位图标
    lcd_show_unit_icon(is_metric);
    
    // 6. 显示模式图标
    lcd_show_mode_icon((MeasurementMode_TypeDef)ZTJ_GetCurrentMode());
    
    // 7. 显示公差图标
    if(measurement_state.mode == MEASUREMENT_MODE_TOL_SETTING) {
        // 公差设置模式特殊处理
        // 显示当前设置的公差值和对应图标
    } else {
        // 非设置模式按原逻辑处理
        lcd_show_tolerance_icon(display_value);
    }
    
    // 8. 显示模拟指针
    // 根据当前模式决定是否显示指针
    
    // 9. 发起显示更新
    LCDDisplay_Update_New();
}
```

## 9. 显示更新机制

### 9.1 缓冲区更新策略
```c
void LCDDisplay_Update_New(void)
{
    // 只更新变化的位，提高效率
    for(int com_index = 0; com_index < 4; com_index++) {
        int ram_index = com_index * 2;
        uint32_t current_ram_value = hlcd.Instance->RAM[ram_index];
        uint32_t new_value = lcd_display_buffer_new[com_index];
        
        uint32_t changed_bits = current_ram_value ^ new_value;
        
        if(changed_bits != 0) {
            HAL_LCD_Write(&hlcd, ram_index, changed_bits, new_value);
        }
    }
    HAL_LCD_UpdateDisplayRequest(&hlcd);
}
```

### 9.2 小数点控制
```c
static void set_decimal_point_for_position(LCD_Position_TypeDef pos, uint8_t enabled)
{
    switch(pos) {
        case LCD_POSITION_1: lcd_set_ram_bit_internal(3, 2, enabled); break;  // COM4/SEG2
        case LCD_POSITION_2: lcd_set_ram_bit_internal(3, 4, enabled); break;  // COM4/SEG4
        case LCD_POSITION_3: lcd_set_ram_bit_internal(3, 6, enabled); break;  // COM4/SEG6
        case LCD_POSITION_4: lcd_set_ram_bit_internal(3, 8, enabled); break;  // COM4/SEG8
        case LCD_POSITION_5: lcd_set_ram_bit_internal(3, 10, enabled); break; // COM4/SEG10
        case LCD_POSITION_6: lcd_set_ram_bit_internal(3, 12, enabled); break; // COM4/SEG12
    }
}
```

## 10. 状态同步机制

### 10.1 测量状态机同步
```c
// 从测量状态机获取当前模式
MeasurementMode_TypeDef current_mode = (MeasurementMode_TypeDef)ZTJ_GetCurrentMode();

// 单位同步检查
extern ZTJ_TypeDef measurement_state;
uint8_t expected_is_metric = (measurement_state.unit == 0) ? 1 : 0;
if (is_metric != expected_is_metric) {
    // 同步不一致，更新LCD显示状态机
    lcd_display_state_ptr->is_metric = expected_is_metric;
    is_metric = expected_is_metric;
}
```

### 10.2 按键状态机同步
```c
// 按键事件处理后更新显示
if(key_event_flags.bits.s_tol_evt) {
    LCDDisplay_ShowIcon_New(LCD_ICON_TOL, 1);
    key_fsm.bits.s_tol = 1;
    key_event_flags.bits.s_tol_evt = 0;
}
```

## 11. 主要函数接口

### 11.1 核心显示函数
```c
// 初始化LCD显示
void LCDDisplay_Init_New(void);

// 清除显示
void LCDDisplay_Clear_New(void);

// 显示字符
void LCDDisplay_ShowChar_New(LCD_Char_TypeDef ch, LCD_Position_TypeDef pos);

// 显示字符串
void LCDDisplay_ShowString_New(const char* str);

// 显示图标
void LCDDisplay_ShowIcon_New(LCD_Icon_TypeDef icon, uint8_t enable);

// 更新显示
void LCDDisplay_Update_New(void);

// 统一显示接口
void LCDDisplay_ShowProcessedData(SensorData_TypeDef processed_data);
```

### 11.2 状态机控制函数
```c
// 显示状态机初始化
void LCD_DisplayState_Init(void);

// 设置显示参数
void LCD_DisplayState_SetMetric(uint8_t is_metric);
void LCD_DisplayState_SetUnit(uint8_t unit);
void LCD_DisplayState_SetMode(uint8_t mode);
void LCD_DisplayState_SetResolution(uint8_t resolution);
void LCD_DisplayState_SetDecimalPosition(uint8_t position);

// 图标控制
void LCD_DisplayState_SetIcon(LCD_Icon_TypeDef icon, uint8_t enable);
uint8_t LCD_DisplayState_GetIconStatus(LCD_Icon_TypeDef icon);

// 获取当前状态
LCD_DisplayState_TypeDef* LCD_DisplayState_GetCurrent(void);
```

## 12. 性能特性

### 12.1 显示优化
- **增量更新**: 只更新变化的显示位
- **缓冲区管理**: 双缓冲机制，避免显示闪烁
- **前导零消除**: 智能消除前导零，提高显示可读性
- **小数点优化**: 根据分辨率和单位自动计算小数点位置

### 12.2 内存使用
- **显示缓冲区**: 4个32位寄存器 (16字节)
- **状态管理**: LCD显示状态结构体 (约220字节)
- **图标状态**: 256位图标状态数组

## 13. 使用示例

### 13.1 初始化
```c
// 在main函数中初始化
LCDDisplay_Init_New();
LCD_DisplayState_Init();
```

### 13.2 基本显示
```c
// 显示数字
LCDDisplay_ShowChar_New(LCD_CHAR_1, LCD_POSITION_1);
LCDDisplay_ShowChar_New(LCD_CHAR_2, LCD_POSITION_2);

// 显示字符串
LCDDisplay_ShowString_New("123.45");

// 显示图标
LCDDisplay_ShowIcon_New(LCD_ICON_MM, 1);
LCDDisplay_ShowIcon_New(LCD_ICON_REL, 1);

// 更新显示
LCDDisplay_Update_New();
```

### 13.3 统一显示接口
```c
// 使用处理后的数据自动完成所有显示
SensorData_TypeDef processed_data;
// ... 填充processed_data ...
LCDDisplay_ShowProcessedData(processed_data);
```

## 14. 调试功能

### 14.1 调试宏定义
```c
#define DEBUG_LCD_DISPLAY    // 启用LCD显示调试
#define DEBUG_KEY_SCAN       // 启用按键扫描调试
```

### 14.2 调试输出
- 显示状态变化跟踪
- 按键事件处理调试
- 分辨率和单位切换调试

## 15. 注意事项

1. **硬件连接**: 确保LCD硬件连接正确，COM/SEG引脚配置匹配
2. **初始化顺序**: 先初始化LCD硬件，再初始化显示状态机
3. **内存管理**: 注意显示缓冲区的生命周期，避免内存泄漏
4. **线程安全**: 显示更新应在主循环中进行，避免中断中直接操作
5. **性能优化**: 大量显示更新时使用增量更新机制
6. **状态同步**: 确保显示状态机与测量状态机、按键状态机同步